.settings-provider {
    padding-top: 8px;
    padding-right: 8px;
    padding-bottom: 8px;
    padding-left: 8px;
}

.title {
    -unity-font-style: bold;
    font-size: var(--unity-metrics-default-font_very_big_size);
}
